const Button = (props) => {
  return (
    <div className="btn-container">
      <button className="see-more-btn">
        <span>See More</span>
      </button>
    </div>
  );
};

export default Button;
